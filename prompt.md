 your task is to create a RAG explorer for the Advance RAG. Make sure that you use a Claude theme for
 that. Prepare a UI.  What I want you to do is stage one, which is the ingestion. Make sure I can upload all my test cases there. I
 will be uploading them as a CSV file or Excel file where you will do an ingestion based on the open source
 stack that you already know. We are going with the open source model right now. I want you to basically, whenever I upload my Excel file, suppose my Excel file has 2,000 test cases in the CSV
 format or Excel format. Whenever I upload them, you need to show me a preview of how this data is put into the
 chunks, into the vector database, and how the chunking was done by the algorithm. How it is put into the
 vector database also. If possible, create a UI also so I can see the data. In this case, how the chunks are stored, how many chunks are stored, how they are properly stored, what is the
 overlapping—every detail I want to see to understand how my test cases are put into the vector database. This
 is the part. After that, I want you to basically show me if I type something from a query. So chat. Suppose I'm using a
 chatbot for that. If I ask any questions or ask him to create a new test case for a Jira ID or anything, you
 will tell me how many chunks are fetched, how many chunks are re-ranked, and what is the result that you are
 coming up with using the Groq API. Create a beautiful UI with a left-hand side panel and right-hand side. I
 will see each and every result step by step process to understand how ingestion happened, how retrieval will
 happen, what are the chunks which come, how many re-ranked, and how the result came to the picture in the end
 properly from the LLM which I have given you also. The LLM we are going to use is an open source LLM. You will see the API key available in the environment
 variable. If you don't see it, you can ask me the key for GROQ. I will give you the key also in the end.





